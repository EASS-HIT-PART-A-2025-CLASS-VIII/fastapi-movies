# 📊 SpendWise Frontend

A modern, high-performance financial dashboard built with **React**, **TypeScript**, and **Vite**. This interface provides real-time data visualization of expenditures and direct interaction with the SpendWise AI Neural Advisor.

---

## 🚀 Quick Start

### 1. Installation

Ensure you have **Node.js (v18+)** and **npm** or **bun** installed.

```bash
# Install dependencies
npm install
```

### 2. Configuration

Create a `.env` file in the frontend root directory to point to your backend API:

```env
VITE_API_URL=http://localhost:8000
```

### 3. Development

Start the development server with Hot Module Replacement (HMR):

```bash
npm run dev
```

📍 Local URL: http://localhost:5173

---

## 🔐 Key Features

- **Real-time Analytics**: Interactive charts showing spending categories and daily burn rates.
- **AI Chat Interface**: Direct communication line with the backend AI Advisor for financial coaching.
- **Transaction Management**: Intuitive forms for logging and categorizing new financial telemetry.
- **Report Center**: Trigger and download automated PDF financial statements generated by the background workers.

---

## 🏗 Project Structure

- **src/components/**: Reusable UI components (Charts, Tables, Modals).
- **src/hooks/**: Custom React hooks for API data fetching and state management.
- **src/services/**: API client configurations (Axios/Fetch) to communicate with the FastAPI backend.
- **src/pages/**: Main view components (Dashboard, Login, Analytics).
- **src/store/**: Global state management for user authentication and session persistence.

---

## 🛠 Tech Stack

- **React 19**: UI library utilizing the latest concurrent features.
- **TypeScript**: Type-safe development for enterprise-grade stability.
- **Vite**: Ultra-fast build tool and development server.
- **Tailwind CSS**: Utility-first CSS framework for responsive design.
- **TanStack Query**: Powerful asynchronous state management for API synchronization.
- **Lucide React**: Clean and consistent iconography.

---

## 🧪 Production Build

To build the application for production deployment:

```bash
npm run build
```

The optimized output will be located in the `dist/` folder, ready to be served by Nginx or hosted on a CDN.
